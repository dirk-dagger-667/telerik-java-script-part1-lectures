String.prototype.stringCount = function (stringToCheck) {

    this = this.toLowerCase();
    stringToCheck = stringToCheck.toLowerCase();
    var len = this.length;
    var toCheckLength = stringToCheck.length;

    for (var i = 0; i < len - toCheckLength; i++) {

        for (var j = 0; j < toCheckLength; j++) {



        }

    }

}